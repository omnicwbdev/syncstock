services:
  php-cli:
    build:
      context: .
      dockerfile: Dockerfile.php8.4
    container_name: sync
    volumes:
      - ./:/app
    env_file:
      - .env  # Carrega variáveis do .env
    environment:
      - TZ=America/Sao_Paulo
      - DEBUG_MODE=true
    working_dir: /app
